import { CircularProgress, type CircularProgressProps } from '@mui/material';

const CCircularProgress: React.FC<CircularProgressProps> = ({...rest} ) => {
    return <CircularProgress {...rest}/>
}
export default CCircularProgress