# Getting Started

### Start All Services with prod mode
    **** Run all microfrontends ****
    cd docker
    run : docker-compose build --no-cache
    run : docker compose up -d

### Removed All Services
    run: docker compose down